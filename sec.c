#include <stdio.h>
int main()
{
    int a;
    float f,sum;
    a=4;
    f=3.6;
    sum=a+f;
    printf("the value of a is %d \n", a);
    printf("the value of f is %f \n", f);
    printf("sum of a and f is %f \n", a + f);
    return 0;
}