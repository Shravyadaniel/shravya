#include <stdio.h>
int main()
{
    int a;
    char c,sum;
    a=7;
    c='s';
    sum=a+c;
    printf("the value of a is %d \n", a);
    printf("the value of c is %c \n", c);
    printf("sum of a and c is %c \n", a + c);
    return 0;
}